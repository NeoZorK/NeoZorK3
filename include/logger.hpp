#ifndef NEOZORK3_LOGGER_HPP
#define NEOZORK3_LOGGER_HPP

#include <string> // Вероятно понадобится для сообщений

namespace neozork::utils {

    // TODO: Объявить функции логирования здесь
    // void log_info(const std::string& message);
    // void log_error(const std::string& message);
    // void log_debug(const std::string& message);

} // namespace neozork::utils

#endif // NEOZORK3_LOGGER_HPP
